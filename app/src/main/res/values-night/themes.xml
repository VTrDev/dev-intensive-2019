<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent_night</item>

        <item name="colorAccentedSurface">@color/color_divider_light</item>
        <item name="colorIcon">@android:color/white</item>
        <item name="colorDivider">@color/color_divider_dark</item>
        <item name="android:statusBarColor">@android:color/black</item>

        <item name="colorActionBarBackground">@color/color_gray_darkest2</item>
        <item name="colorPrimaryBackground">@android:color/black</item>
        <item name="colorChatItemSwipeBackground">@color/color_accent_night</item>
        <item name="colorChatIndicator">@color/color_accent_night</item>
        <item name="colorChatItemTitle">@android:color/white</item>
        <item name="colorChatItemText">@color/color_gray</item>
        <item name="colorChatItemMessageCount">@android:color/white</item>
        <item name="colorChatItemMessageCountBadge">@color/color_gray_dark</item>
        <item name="colorGroupItemCheckIcon">@color/color_accent_night</item>

    </style>

</resources>