<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        
        <item name="colorAccentedSurface">@color/color_primary_light</item>
        <item name="colorIcon">@color/color_gray</item>
        <item name="colorDivider">@color/color_divider_dark</item>

        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowAnimationStyle">@style/ActivityTransitions</item>

        <item name="colorActionBarBackground">@color/color_primary</item>
        <item name="colorPrimaryBackground">@android:color/white</item>
        <item name="colorChatItemBackground">@android:color/white</item>
        <item name="colorChatItemSwipeBackground">@color/color_primary_dark</item>
        <item name="colorChatIndicator">@color/color_online_indicator</item>
        <item name="colorChatItemTitle">@color/color_primary</item>
        <item name="colorChatItemText">@color/color_gray_dark</item>
        <item name="colorChatItemMessageCount">@android:color/white</item>
        <item name="colorChatItemMessageCountBadge">@color/color_count_badge</item>

    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
    
</resources>